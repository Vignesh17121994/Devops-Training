# Setup Hostname 
sudo hostnamectl set-hostname "web.cloudbinary.io"

# Update the hostname part of Host File
echo "`hostname -I | awk '{ print $1 }'` `hostname`" >> /etc/hosts 

# Create a File 
sudo echo "Today Date is `date`" >> /opt/devops.txt  

# Download Install & configure Softwares 
sudo yum install git curl wget tree unzip -y 

# To Restart SSM Agent on Ubuntu 
# sudo systemctl restart snap.amazon-ssm-agent.amazon-ssm-agent.service

#  To Restart SSM Agent on AmazonLinux 
sudo systemctl restart amazon-ssm-agent

# Attach Instance profile To EC2 Instance 
# aws ec2 associate-iam-instance-profile \
--iam-instance-profile Name=SA-EC2-SSM \
--instance-id "i-04707ff7a89bb9525"

# To terminate an Amazon EC2 instance
# aws ec2 terminate-instances --instance-ids "i-0d3b907eb546a8dfd"
